<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.DetailMapper">
    <cache/>
    <!-- 查询 -->
    <select id="queryDetail" parameterType="" resultType="com.model.pojo.Detail">

    </select>
    <!-- 新增 -->
    <insert id="insertDetail" parameterType="" useGeneratedKeys="true" keyColumn="" keyProperty="">

    </insert>
    <!-- 修改 -->
    <update id="updateDetail" parameterType="">
        update
        <set>
            <if test="">

            </if>

        </set>
        where
    </update>

</mapper>