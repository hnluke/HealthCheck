<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.BatchesMapper">
    <cache/>
    <!-- 查询 -->
    <select id="findBatches" parameterType="" resultType="">

    </select>
    <!-- 新增 -->
    <insert id="insertBatches" parameterType="" useGeneratedKeys="true" keyColumn="" keyProperty="">

    </insert>
    <!-- 修改 -->
    <update id="updateBatches" parameterType="">
        update
        <set>
            <if test="">

            </if>

        </set>
        where
    </update>


</mapper>