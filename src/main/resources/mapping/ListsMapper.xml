<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.ListsMapper">
    <cache/>
    <!-- 查询 -->
    <select id="queryLists" parameterType="" resultType="com.model.pojo.Lists">

    </select>
    <!-- 新增 -->
    <insert id="insertLists" parameterType="" useGeneratedKeys="true" keyColumn="" keyProperty="">

    </insert>
    <!-- 修改 -->
    <update id="updateLists" parameterType="">
        update
        <set>
            <if test="">

            </if>

        </set>
        where
    </update>
    <!-- 删除 -->
    <delete id="deleteLists" parameterType="">

    </delete>

</mapper>