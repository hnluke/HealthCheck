<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.BriefsMapper">
    <cache/>
    <!-- 查询 -->
    <select id="findBriefs" parameterType="" resultType="com.model.pojo.Briefs">

    </select>
    <!-- 新增 -->
    <insert id="insertBriefs" parameterType="" useGeneratedKeys="true" keyColumn="" keyProperty="">

    </insert>
    <!-- 修改 -->
    <update id="updateBriefs" parameterType="">
        update
        <set>
            <if test="">

            </if>

        </set>
        where
    </update>

</mapper>